includes:
  - "./running-openjdk-base.yml"

configs:
  # heap16g, matching running-openjdk-nogc-complete.yml's MMTk NoGC heap -
  # openjdk-plot.yml plots this as NoGC's baseline, so a heap-size mismatch
  # here would make that comparison meaningless.
  - "jdk-stock|common|epsilon|heap16g"
