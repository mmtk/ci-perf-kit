includes:
    - "../running-ng/config/base/suites.yml"
    - "./ci_modifiers.yml"

overrides:
    "suites.dacapochopin-69a704e.timeout": 240
    "suites.dacapochopin-69a704e.callback": "probe.DacapoChopinCallback"

# Can override with command line argument -i
invocations: 5
# Can override with command line argument -m
minheap_multiplier: 1.0
heap_range: 6
spread_factor: 1
remote_host: